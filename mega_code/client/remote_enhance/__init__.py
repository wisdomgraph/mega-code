"""Remote skill-enhancement client — packages, uploads, polls, installs.

Entry point is ``python -m mega_code.client.remote_enhance`` (see
``__main__.py``). Submodules are independent so each can be unit-tested
without touching the network or the filesystem outside its own scope.
"""
